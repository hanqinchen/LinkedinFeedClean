# LinkedIn Feed Categorizer - 产品说明文档

## 1. 产品概述

### 1.1 问题描述

LinkedIn 信息流将所有关注对象（人、公司主页、话题）的帖子混合展示，用户无法按主题或兴趣领域筛选内容。当关注数量增多后，有价值的信息被淹没在大量无关内容中，信息获取效率低下。

### 1.2 解决方案

一款 Chrome 浏览器扩展，允许用户：

- 将关注的人/主页归入自定义分类（如"AI/ML"、"创业"、"投资"）
- 在 LinkedIn 信息流页面通过分类标签一键切换，只查看某个分类下的动态
- 利用 AI 根据关注列表的个人简介自动建议分类，用户确认后完成分类

### 1.3 目标用户

LinkedIn 重度使用者，关注大量行业人物和公司，希望按主题高效消费信息流内容。

---

## 2. 产品形态

| 属性 | 说明 |
|------|------|
| 形态 | Chrome 浏览器扩展 |
| 技术规范 | Manifest V3 |
| AI 能力 | Claude API（Anthropic） |
| 数据存储 | chrome.storage.sync（跨设备同步） |
| 目标平台 | Chrome / Edge / Brave 等 Chromium 内核浏览器 |

---

## 3. 功能详细说明

### 3.1 信息流分类筛选栏

**位置**：注入到 LinkedIn 信息流页面顶部（排序选项下方）

**UI 形态**：水平标签栏

```
┌──────────────────────────────────────────────────────┐
│  [全部]  [AI/ML]  [创业]  [投资]  [技术]  [⚙ 管理]  │
└──────────────────────────────────────────────────────┘
```

**交互行为**：

| 操作 | 行为 |
|------|------|
| 点击"全部" | 显示所有帖子（默认状态） |
| 点击某个分类标签 | 隐藏不属于该分类成员的帖子，仅显示该分类成员的动态 |
| 点击"⚙ 管理" | 打开扩展 Popup 进行分类管理 |
| 选中状态 | 当前激活的分类标签高亮显示 |

**技术要点**：
- 使用 `MutationObserver` 监听 LinkedIn 信息流容器，自动处理无限滚动加载的新帖子
- 通过帖子 DOM 结构提取作者的姓名和主页链接，与分类成员列表匹配
- 不匹配的帖子通过 CSS `display: none` 隐藏，不破坏 LinkedIn 的页面布局

### 3.2 分类管理

**入口**：点击扩展图标打开 Popup，或点击信息流中的"⚙ 管理"按钮

#### 3.2.1 分类 CRUD

| 操作 | 说明 |
|------|------|
| 创建分类 | 输入分类名称，选择颜色标识 |
| 重命名分类 | 编辑已有分类的名称 |
| 修改颜色 | 更换分类的颜色标识 |
| 删除分类 | 删除分类及其所有成员关联（成员本身不受影响） |
| 排序 | 拖拽调整分类在标签栏中的显示顺序 |

#### 3.2.2 成员管理

| 操作 | 说明 |
|------|------|
| 添加成员 | 在分类中搜索并添加已关注的人/主页 |
| 移除成员 | 从分类中移除某个人/主页 |
| 批量操作 | 选中多个成员进行批量添加/移除 |
| 查看成员 | 展示分类下所有成员列表（姓名 + 简要信息） |

**一个人可以属于多个分类**（例如某人既是 AI 领域专家又是创业者）。

#### 3.2.3 快捷操作 - 右键菜单

在 LinkedIn 信息流中，右键点击某个帖子的作者头像或姓名：

```
┌─────────────────────┐
│ 添加到分类...        │
│  ├─ AI/ML           │
│  ├─ 创业            │
│  ├─ 投资            │
│  └─ + 新建分类      │
│ 从分类中移除...      │
└─────────────────────┘
```

### 3.3 AI 智能分类建议

**入口**：Popup 中的"AI 智能分类"按钮

**完整流程**：

```
步骤 1: 用户点击"AI 智能分类"
    │
步骤 2: 扩展自动访问 LinkedIn 关注列表页面
        提取关注的人的信息（姓名、职位、公司、简介）
    │
步骤 3: 将提取的信息发送至 Claude API
        AI 分析每个人的特征，建议分类名称和分组
    │
步骤 4: 展示 AI 建议的审核界面
    │
    ├── 用户可以：
    │   ├── 接受建议的分类名称或修改
    │   ├── 调整某个人所属的分类
    │   ├── 合并/拆分建议的分类
    │   └── 忽略不想分类的人
    │
步骤 5: 用户点击"确认"，保存分类结果
```

**AI 建议审核界面示意**：

```
┌─────────────────────────────────────────────┐
│         AI 分类建议                          │
│                                             │
│  建议分类: AI/ML (15人)                      │
│  ┌─ ✅ Andrej Karpathy - 前 Tesla AI 总监   │
│  ├─ ✅ Yann LeCun - Meta 首席 AI 科学家     │
│  ├─ ❌ Elon Musk - (移除)                   │
│  └─ ...                                     │
│                                             │
│  建议分类: 创业/投资 (8人)                    │
│  ┌─ ✅ Paul Graham - Y Combinator 联合创始人 │
│  ├─ ✅ Reid Hoffman - LinkedIn 联合创始人    │
│  └─ ...                                     │
│                                             │
│  [合并分类]  [拆分分类]  [确认保存]           │
└─────────────────────────────────────────────┘
```

### 3.4 设置页面

**入口**：扩展 Options 页面

| 设置项 | 说明 |
|--------|------|
| Claude API Key | 用于 AI 分类建议功能，加密存储 |
| 过滤模式 | "隐藏"模式（完全隐藏不匹配帖子）或"淡化"模式（降低不匹配帖子的不透明度） |
| 数据导出 | 导出分类数据为 JSON 文件 |
| 数据导入 | 从 JSON 文件导入分类数据 |

---

## 4. 技术架构

### 4.1 项目结构

```
linkedin-widget/
├── manifest.json              # Chrome 扩展配置 (Manifest V3)
├── content/
│   ├── content.js             # 注入 LinkedIn 页面的内容脚本
│   └── content.css            # 注入页面的样式
├── popup/
│   ├── popup.html             # 扩展弹窗界面
│   ├── popup.js               # 弹窗交互逻辑
│   └── popup.css              # 弹窗样式
├── background/
│   └── background.js          # Service Worker（处理 AI API 调用）
├── options/
│   ├── options.html           # 设置页面
│   └── options.js             # 设置页面逻辑
├── lib/
│   └── storage.js             # 存储层封装（chrome.storage.sync 的读写）
└── icons/
    ├── icon16.png
    ├── icon48.png
    └── icon128.png
```

### 4.2 数据模型

```json
{
  "categories": [
    {
      "id": "cat_1694500000",
      "name": "AI/ML",
      "color": "#4A90D9",
      "order": 0,
      "members": [
        {
          "name": "Andrej Karpathy",
          "profilePath": "/in/karpathy",
          "title": "前 Tesla AI 总监"
        }
      ]
    },
    {
      "id": "cat_1694500001",
      "name": "创业",
      "color": "#7B68EE",
      "order": 1,
      "members": []
    }
  ],
  "settings": {
    "claudeApiKey": "sk-ant-api03-xxx",
    "filterMode": "hide",
    "activeCategory": null
  }
}
```

### 4.3 组件职责

| 组件 | 职责 |
|------|------|
| **Content Script** | 注入分类标签栏 UI；监听帖子加载；提取帖子作者信息；按分类过滤帖子；注册右键菜单 |
| **Popup** | 分类 CRUD 界面；成员管理；AI 分类建议的触发和审核界面 |
| **Background Service Worker** | 处理 Claude API 调用（API Key 不暴露给内容脚本）；协调存储操作 |
| **Options Page** | API Key 配置；过滤模式设置；数据导入导出 |
| **Storage (lib)** | 封装 chrome.storage.sync 的读写操作；提供统一的数据访问接口 |

### 4.4 关键技术方案

#### 4.4.1 帖子作者识别

LinkedIn 信息流中的帖子结构（需根据实际 DOM 调整）：

- 通过 `[data-urn]` 属性定位帖子容器
- 通过 `.feed-shared-actor` 或类似选择器定位作者区域
- 提取作者姓名（文本内容）和个人主页链接（`href` 属性中的 `/in/xxx` 路径）
- 用作者主页路径作为唯一标识符与分类成员匹配

#### 4.4.2 无限滚动处理

```javascript
const observer = new MutationObserver((mutations) => {
  for (const mutation of mutations) {
    for (const node of mutation.addedNodes) {
      // 检测新加载的帖子，应用当前分类过滤
    }
  }
});
observer.observe(feedContainer, { childList: true, subtree: true });
```

#### 4.4.3 关注列表提取

访问 LinkedIn 关注列表页面 (`/mynetwork/network-manager/people-follow/following/`)：
- 提取每个关注对象的姓名、职位、简介
- 处理分页加载（滚动到底部触发加载更多）
- 将提取的数据传递给 Background Service Worker 进行 AI 分析

#### 4.4.4 Claude API 调用

在 Background Service Worker 中调用 Claude API：

```
System: 你是一个帮助用户整理 LinkedIn 关注列表的助手。
根据以下关注列表中每个人的姓名、职位和简介，
将他们分入合理的主题类别。

要求：
1. 类别名称简洁明了（2-4个字）
2. 每人至少归入一个类别，可以属于多个类别
3. 建议 3-8 个类别
4. 输出 JSON 格式

User: [关注列表数据]
```

### 4.5 存储限制与应对

| 约束 | 说明 |
|------|------|
| chrome.storage.sync 总量 | 100KB |
| 单个 key 限制 | 8KB |
| 预估每个成员数据 | ~100 字节 |
| 可支持成员数 | 约 500-800 人 |

**应对策略**：
- 成员数据仅存储必要字段（name、profilePath、title）
- 如数据接近上限，提示用户清理或使用导出/导入功能
- 大数据量场景降级使用 `chrome.storage.local`（不同步但无容量限制）

---

## 5. 用户交互流程

### 5.1 首次使用流程

```
1. 安装扩展 → 打开 LinkedIn
2. 信息流顶部出现提示"欢迎使用 LinkedIn Feed Categorizer"
3. 点击"开始设置" → 打开 Popup
4. （可选）配置 Claude API Key
5. 手动创建第一个分类 或 使用 AI 智能分类
6. 回到信息流，点击分类标签开始使用
```

### 5.2 日常使用流程

```
1. 打开 LinkedIn → 信息流顶部显示分类标签栏
2. 点击感兴趣的分类 → 信息流自动过滤
3. 浏览完毕后点击"全部"回到完整信息流
4. 遇到新关注的人 → 右键头像 → 添加到分类
```

### 5.3 AI 分类流程

```
1. 打开 Popup → 点击"AI 智能分类"
2. 扩展自动提取关注列表（显示进度）
3. AI 生成分类建议（显示加载动画）
4. 审核界面：调整分类名称、移动成员、合并/拆分分类
5. 点击"确认保存" → 分类生效
```

---

## 6. 实现计划

| 阶段 | 内容 | 验证标准 |
|------|------|----------|
| 1. 项目脚手架 | manifest.json + 基础文件结构 | 扩展可加载到 Chrome，Popup 能打开 |
| 2. 存储层 | storage.js 封装 CRUD 操作 | 分类数据正确读写 chrome.storage.sync |
| 3. 分类标签栏 | Content Script 注入 UI | LinkedIn 页面顶部出现标签栏 |
| 4. 帖子过滤 | 作者识别 + 过滤逻辑 | 选择分类后正确隐藏/显示帖子 |
| 5. Popup 管理界面 | 分类和成员的 CRUD 界面 | 可创建、编辑、删除分类和成员 |
| 6. 右键菜单 | 快捷添加到分类 | 右键帖子作者可添加到分类 |
| 7. AI 分类建议 | 关注列表提取 + Claude API + 审核界面 | 端到端完成 AI 建议和确认流程 |
| 8. 设置页面 | API Key 配置 + 过滤模式 + 导入导出 | 各项设置生效 |
| 9. 样式打磨 | UI 与 LinkedIn 风格协调 | 视觉上自然融入 LinkedIn 页面 |

---

## 7. 风险与约束

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| LinkedIn DOM 结构变更 | 帖子识别和过滤失效 | 使用多层兜底选择器；发现失效时提示用户更新扩展 |
| chrome.storage.sync 容量不足 | 无法存储大量成员 | 数据精简 + local storage 降级 + 导入导出 |
| LinkedIn 反爬措施 | 关注列表提取可能被限制 | 控制提取速度；提供手动添加成员的替代方案 |
| Claude API 费用 | AI 分类建议产生 API 调用费用 | 仅在用户主动触发时调用；显示预估 token 消耗 |
